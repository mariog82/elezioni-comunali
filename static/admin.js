let mayorChart=null,listChart=null,lastData=null;
async function api(url, options={}){const res=await fetch(url,{credentials:"include",headers:{"Content-Type":"application/json"},...options});const data=await res.json();if(!res.ok||data.ok===false)throw new Error(data.error||"Errore server");return data}
async function loadDashboard(){try{const d=await api("/api/dashboard");lastData=d;prepareSettings(d);draw("mayorChart",d.mayors.map(x=>x.name),d.mayors.map(x=>x.total||0),"mayor");draw("listChart",d.lists.map(x=>x.name),d.lists.map(x=>x.total||0),"list");const tb=document.getElementById("sections");tb.innerHTML="";d.sections.forEach(s=>{const tr=document.createElement("tr");tr.innerHTML=`<td>${s.section}</td><td>${s.representative}</td><td>${s.total_mayors||0}</td><td>${s.total_lists||0}</td><td>${s.total_preferences||0}</td><td>${s.updated_at}</td>`;tb.appendChild(tr)});renderPrefs(d);renderElected(d);await loadUsers()}catch(e){alert(e.message+"\nAccedi come amministratore.");location.href="/"}}
function prepareSettings(d){const s=d.election.settings;document.getElementById("totalElectors").value=s.total_electors;document.getElementById("totalVoters").value=s.total_voters;document.getElementById("councilSeats").value=s.council_seats;const sel=document.getElementById("winnerMayor");if(!sel.dataset.ready){d.data.mayors.forEach(m=>{const o=document.createElement("option");o.value=m;o.textContent=m;sel.appendChild(o)});sel.dataset.ready="1"}sel.value=s.winner_mayor;document.getElementById("mode").value=s.mode}
async function saveSettings(){try{await api("/api/settings",{method:"POST",body:JSON.stringify({total_electors:document.getElementById("totalElectors").value,total_voters:document.getElementById("totalVoters").value,council_seats:document.getElementById("councilSeats").value,winner_mayor:document.getElementById("winnerMayor").value,mode:document.getElementById("mode").value})});alert("Parametri salvati");await loadDashboard()}catch(e){alert(e.message)}}
function draw(id,labels,values,kind){if(kind==="mayor"&&mayorChart)mayorChart.destroy();if(kind==="list"&&listChart)listChart.destroy();const chart=new Chart(document.getElementById(id),{type:"bar",data:{labels,datasets:[{data:values,label:"Voti"}]},options:{responsive:true,plugins:{legend:{display:false}}}});if(kind==="mayor")mayorChart=chart;else listChart=chart}
function renderPrefs(d){const box=document.getElementById("prefTables");box.innerHTML="";Object.keys(d.data.lists).forEach(l=>{const rows=d.preferences.filter(x=>x.list_name===l).sort((a,b)=>(b.total||0)-(a.total||0)).slice(0,10);let html=`<h3>${l}</h3><div class="tablewrap"><table><tr><th>Candidato</th><th>Preferenze</th></tr>`;rows.forEach(r=>html+=`<tr><td>${r.name}</td><td>${r.total||0}</td></tr>`);html+="</table></div>";const div=document.createElement("div");div.className="card";div.innerHTML=html;box.appendChild(div)})}
function renderElected(d){const e=d.election;const s=e.settings;let html=`<p><b>Elettori:</b> ${s.total_electors} &nbsp; <b>Votanti:</b> ${s.total_voters} &nbsp; <b>Affluenza:</b> ${s.total_electors?((s.total_voters/s.total_electors)*100).toFixed(2):"0.00"}%</p>`;html+=`<p><b>Sindaco/coalizione vincente impostata:</b> ${s.winner_mayor}<br><b>Premio di maggioranza:</b> ${e.premium_applied?"APPLICATO":"NON applicato"} ${e.premium_applied?`(${e.premium_seats} seggi)`:""}<br><span class="small">Coalizione vincente: ${e.winner_pct.toFixed(2)}% delle liste ammesse. Maggior altra coalizione: ${e.other_max_pct.toFixed(2)}%.</span></p>`;html+=`<h3>Seggi per coalizione</h3><div class="tablewrap"><table><tr><th>Coalizione</th><th>Voti liste ammesse</th><th>Seggi</th></tr>`;Object.entries(e.coalition_seats).sort((a,b)=>b[1]-a[1]).forEach(([c,seats])=>html+=`<tr><td>${c}</td><td>${e.coalition_votes[c]||0}</td><td><b>${seats}</b></td></tr>`);html+=`</table></div>`;html+=`<h3>Seggi per lista e consiglieri eletti</h3><div class="tablewrap"><table><tr><th>Lista</th><th>Coalizione</th><th>Voti lista</th><th>Seggi</th><th>Eletti simulati</th></tr>`;Object.entries(d.data.lists).forEach(([l,obj])=>{const seats=e.list_seats[l]||0;const elected=(e.elected[l]||[]).map(x=>`<div class="elected">${x.name} (${x.votes})</div>`).join("");const under=(e.list_votes[l]||0)>0 && !e.admitted_lists[l] ? ` <span class="badge">sotto soglia 5%</span>`:"";html+=`<tr><td>${l}${under}</td><td>${obj.coalition}</td><td>${e.list_votes[l]||0}</td><td><b>${seats}</b></td><td>${elected||"<span class='muted'>Nessun seggio</span>"}</td></tr>`});html+=`</table></div>`;if(e.losing_mayors.length){html+=`<p class="small"><b>Nota:</b> sindaci non eletti ordinati per voti: ${e.losing_mayors.map(x=>`${x.name} (${x.votes})`).join(", ")}. La proclamazione ufficiale resta di competenza degli uffici elettorali.</p>`}document.getElementById("electedBox").innerHTML=html}
async function loadUsers(){const d=await api("/api/users");const box=document.getElementById("users");box.innerHTML="";d.users.forEach(u=>{const link=`${location.origin}/?token=${u.qr_token}`;const div=document.createElement("div");div.className="card";div.innerHTML=`<b>${u.name}</b><br>Codice: ${u.phone}<br>Ruolo: ${u.role}<br>Sezione: ${u.section||"tutte"}<br>Stato: ${u.active?"attivo":"disattivato"}<br><label>Link QR/accesso</label><input value="${link}" readonly onclick="this.select()"><button class="secondary" onclick="toggleUser(${u.id})">${u.active?"Disattiva":"Riattiva"}</button><button class="danger" onclick="deleteUser(${u.id})">Rimuovi</button>`;box.appendChild(div)})}
async function createUser(){try{await api("/api/users",{method:"POST",body:JSON.stringify({name:document.getElementById("newName").value.trim(),phone:document.getElementById("newPhone").value.trim(),pin:document.getElementById("newPin").value.trim(),section:document.getElementById("newSection").value.trim(),role:document.getElementById("newRole").value})});alert("Utente creato");await loadUsers()}catch(e){alert(e.message)}}
async function deleteUser(id){if(!confirm("Rimuovere definitivamente questo rappresentante/utente?"))return;try{await api(`/api/users/${id}`,{method:"DELETE"});await loadUsers()}catch(e){alert(e.message)}}
async function toggleUser(id){if(!confirm("Cambiare stato attivo/disattivo dell'utente?"))return;try{await api(`/api/users/${id}/toggle`,{method:"PATCH",body:"{}"});await loadUsers()}catch(e){alert(e.message)}}
async function resetVotes(){const c=prompt("Per confermare l'azzeramento di tutti i voti scrivi: AZZERA");if(c!=="AZZERA")return alert("Operazione annullata");try{await api("/api/reset-votes",{method:"POST",body:JSON.stringify({confirm:"AZZERA"})});alert("Voti azzerati");await loadDashboard()}catch(e){alert(e.message)}}
loadDashboard();setInterval(loadDashboard,30000);